mqtt:
  enabled: false

#ffmpeg:
#  hwaccel_args: preset-vaapi

detectors:
  coral1:
    type: edgetpu
    device: pci:0

objects:
  track:
    - person
    - dog

record:
  enabled: true
  retain:
    days: 28
    mode: all

snapshots:
  enabled: false
  timestamp: true
  bounding_box: true
  retain:
    default: 30

birdseye:
  # Optional: Enable birdseye view (default: shown below)
  enabled: false
  # Optional: Width of the output resolution (default: shown below)
  width: 960
  # Optional: Height of the output resolution (default: shown below)
  height: 540
  # Optional: Encoding quality of the mpeg1 feed (default: shown below)
  # 1 is the highest quality, and 31 is the lowest. Lower quality feeds utilize less CPU resources.
  quality: 8
  # Optional: Mode of the view. Available options are: objects, motion, and continuous
  #   objects - cameras are included if they have had a tracked object within the last 30 seconds
  #   motion - cameras are included if motion was detected in the last 30 seconds
  #   continuous - all cameras are included always
  #mode: continuous

cameras:
  front_of_house: # <------ Name the camera
    enabled: true
    ffmpeg:
   #   hwaccel_args: preset-vaapi
      inputs:
        - path: rtsp://admin:D4mn4t10nBugg3r4t10n@10.0.0.81/Preview_01_main #(2560x1440) <----- The stream you want to use for detection
          roles:
            - record
        - path: rtsp://admin:D4mn4t10nBugg3r4t10n@10.0.0.81/Preview_01_sub #(640x360)
          roles:
            - detect
          #  - record
    detect:
      enabled: true # <---- disable detection until you have a working camera feed
      width: 640
      height: 360
    onvif:
      host: 10.0.0.81
      port: 8000
      user: admin
      password: D4mn4t10nBugg3r4t10n

    motion:
      mask: 0.739,0.883,0.741,0.588,0.039,0.392,0.129,0.68
  Kitchen: # <------ Name the camera
    enabled: true
    ffmpeg:
  #    hwaccel_args: preset-vaapi
      inputs:
        - path: 
            rtsp://admin:D4mn4t10nBugg3r4t10n@10.0.0.82:554/cam/realmonitor?channel=1&subtype=0&unicast=true&proto=Onvif     # (2560x1440)<----- The stream you want to use for detection
          roles:
            - detect
            - record
    detect:
      enabled: true # <---- disable detection until you have a working camera feed
      width: 1280
      height: 720
    onvif:
      host: 10.0.0.82
      port: 80
      user: admin
      password: D4mn4t10nBugg3r4t10n

    motion:
      mask: 0.841,0.55,0.653,0.635,0.687,1,0.91,1
  garden: # <------ Name the camera
    enabled: true
    ffmpeg:
   #   hwaccel_args: preset-vaapi
      inputs:
        - path: rtsp://admin:Bugg3r4t10n@10.0.0.83:8554/Streaming/Channels/101 #(2304x1296) <----- The stream you want to use for detection
          roles:
            - detect
            - record
    detect:
      enabled: true # <---- disable detection until you have a working camera feed 
      width: 1280
      height: 720
    onvif:
      host: 10.0.0.83
      port: 8000
      user: admin
      password: Bugg3r4t10n

    motion:
      mask: 0,0.752,0,1,1,1,1,0.619
